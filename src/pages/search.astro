---
import Layout from '~/layouts/PageLayout.astro';

const metadata = {
  title: 'Search',
  description: 'Search through all blog posts, insights, and commentary',
  pagefind: false,
};
---

<Layout metadata={metadata}>
  <section class="px-4 py-16 mx-auto max-w-6xl lg:px-8 lg:py-20">
    <div class="max-w-3xl mx-auto text-center pb-10 md:pb-16">
      <h1 class="text-4xl md:text-5xl font-bold leading-tighter tracking-tighter mb-4 font-heading dark:text-gray-200">
        Search
      </h1>
      <p class="text-xl text-muted dark:text-slate-400">
        Search through all blog posts, insights, and commentary on AI and technology leadership
      </p>
    </div>
    
    <div class="max-w-4xl mx-auto">
      <div id="search" class="w-full"></div>
    </div>
  </section>
</Layout>

<script>
  // Initialize Pagefind UI using Astro's lifecycle event to avoid race conditions
  async function initializeSearch() {
    const searchElement = document.getElementById('search');
    if (!searchElement) return;
    
    // Show loading state
    searchElement.innerHTML = '<div class="p-4 text-center text-gray-500">Loading search...</div>';
    
    let attempts = 0;
    const maxAttempts = 5;
    
    while (attempts < maxAttempts) {
      attempts++;
      
      try {
        // Dynamic import with retry logic
        const { PagefindUI } = await import('@pagefind/default-ui');
        
        // Clear loading state
        searchElement.innerHTML = '';
        
        // Initialize PagefindUI
        new PagefindUI({ 
          element: "#search", 
          showSubResults: true,
          showImages: false,  // Disable images - we'll handle placeholders ourselves
          excerptLength: 30,
          showEmptyFilters: false,
          resetStyles: false
        });
        
        // Setup trailing slash fix after successful initialization
        setupTrailingSlashFix();
        return; // Success - exit retry loop
        
      } catch (error) {
        console.warn(`Search initialization attempt ${attempts}/${maxAttempts} failed:`, error);
        
        if (attempts === maxAttempts) {
          // Final fallback
          searchElement.innerHTML = '<div class="p-4 text-center text-gray-500">Search temporarily unavailable. Please refresh the page.</div>';
        } else {
          // Wait before retry with exponential backoff
          await new Promise(resolve => setTimeout(resolve, 200 * attempts));
        }
      }
    }
  }
  
  function setupTrailingSlashFix() {
    // Fix trailing slash URLs in search results using MutationObserver
    const observer = new MutationObserver((mutations) => {
      mutations.forEach((mutation) => {
        mutation.addedNodes.forEach((node) => {
          if (node.nodeType === Node.ELEMENT_NODE) {
            // Fix links in the added content
            const links = node.querySelectorAll ? node.querySelectorAll('a[href]') : [];
            links.forEach(fixTrailingSlash);
            
            // Also fix if the node itself is a link
            if (node.tagName === 'A' && node.href) {
              fixTrailingSlash(node);
            }
          }
        });
      });
    });
    
    function fixTrailingSlash(link) {
      if (link.href && link.href.includes(window.location.hostname)) {
        const url = new URL(link.href);
        if (url.pathname.endsWith('/') && url.pathname !== '/') {
          const newPath = url.pathname.replace(/\/$/, '');
          link.href = url.origin + newPath + url.search + url.hash;
        }
      }
    }
    
    // Start observing
    const searchElement = document.getElementById('search');
    if (searchElement) {
      observer.observe(searchElement, {
        childList: true,
        subtree: true
      });
      
      // Also fix any existing links after a short delay to catch initial results
      setTimeout(() => {
        document.querySelectorAll('#search a[href]').forEach(fixTrailingSlash);
      }, 100);
    }
  }
  
  // Use Astro's lifecycle event instead of DOMContentLoaded to avoid race conditions
  document.addEventListener('astro:page-load', initializeSearch);
  
  // Also handle initial page load if astro:page-load already fired
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initializeSearch);
  } else {
    initializeSearch();
  }
</script>

<style>
  /* Custom styles for Pagefind to match AstroWind theme */
  :global(.pagefind-ui) {
    --pagefind-ui-scale: 1;
    --pagefind-ui-primary: #3b82f6;
    --pagefind-ui-text: #374151;
    --pagefind-ui-background: #ffffff;
    --pagefind-ui-border: #d1d5db;
    --pagefind-ui-tag: #f3f4f6;
    --pagefind-ui-border-width: 1px;
    --pagefind-ui-border-radius: 0.5rem;
  }

  :global(.dark .pagefind-ui) {
    --pagefind-ui-text: #d1d5db;
    --pagefind-ui-background: #1f2937;
    --pagefind-ui-border: #374151;
    --pagefind-ui-tag: #374151;
  }

  :global(.pagefind-ui__search-input) {
    border-radius: 0.5rem;
    padding: 0.75rem 1rem;
    font-size: 1.125rem;
    color: #1f2937 !important;
    background-color: #ffffff !important;
    border: 2px solid #d1d5db !important;
  }

  :global(.pagefind-ui__search-input:focus) {
    border-color: #3b82f6 !important;
    outline: none !important;
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1) !important;
  }

  :global(.pagefind-ui__search-input::placeholder) {
    color: #6b7280 !important;
  }

  :global(.dark .pagefind-ui__search-input) {
    color: #f9fafb !important;
    background-color: #374151 !important;
    border-color: #4b5563 !important;
  }

  :global(.dark .pagefind-ui__search-input:focus) {
    border-color: #60a5fa !important;
    box-shadow: 0 0 0 3px rgba(96, 165, 250, 0.1) !important;
  }

  :global(.dark .pagefind-ui__search-input::placeholder) {
    color: #9ca3af !important;
  }

  :global(.pagefind-ui__result) {
    border-radius: 0.5rem;
    margin-bottom: 1.5rem;
    padding: 1rem;
    border: 1px solid var(--pagefind-ui-border);
  }

  :global(.pagefind-ui__result:hover) {
    background-color: #f9fafb;
    border-color: #9ca3af;
  }

  :global(.dark .pagefind-ui__result:hover) {
    background-color: #374151;
    border-color: #6b7280;
  }

  :global(.pagefind-ui__result-title) {
    font-weight: 600;
    margin-bottom: 0.5rem;
    font-size: 1.125rem;
  }

  :global(.pagefind-ui__result-link) {
    color: #1f2937;
    text-decoration: none;
  }

  :global(.dark .pagefind-ui__result-link) {
    color: #f9fafb;
  }

  :global(.pagefind-ui__result-link:hover) {
    color: #3b82f6;
  }

  :global(.pagefind-ui__result-excerpt) {
    color: #6b7280;
    line-height: 1.5;
    margin-bottom: 0.5rem;
  }

  :global(.dark .pagefind-ui__result-excerpt) {
    color: #9ca3af;
  }

  /* Style sub-results (anchor links) */
  :global(.pagefind-ui__result-nested) {
    margin-top: 0.5rem;
    padding-left: 1rem;
    border-left: 2px solid #e5e7eb;
  }

  :global(.dark .pagefind-ui__result-nested) {
    border-left-color: #4b5563;
  }

  :global(.pagefind-ui__result-nested-title) {
    display: inline-flex;
    align-items: center;
    font-size: 0.875rem;
    color: #6b7280;
    margin-bottom: 0.25rem;
  }

  :global(.pagefind-ui__result-nested-title::before) {
    content: "↪";
    margin-right: 0.5rem;
    font-size: 0.75rem;
  }

  :global(.dark .pagefind-ui__result-nested-title) {
    color: #9ca3af;
  }

  /* Result metadata styling */
  :global(.pagefind-ui__result-tags) {
    margin-top: 0.5rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  :global(.pagefind-ui__result-tag) {
    font-size: 0.75rem;
    padding: 0.125rem 0.5rem;
    border-radius: 0.25rem;
    background-color: #f3f4f6;
    color: #6b7280;
  }

  :global(.dark .pagefind-ui__result-tag) {
    background-color: #374151;
    color: #9ca3af;
  }

  /* Clear button styling */
  :global(.pagefind-ui__search-clear) {
    background: none;
    border: none;
    color: #6b7280;
    cursor: pointer;
    padding: 0.5rem;
  }

  :global(.pagefind-ui__search-clear:hover) {
    color: #374151;
  }

  :global(.dark .pagefind-ui__search-clear:hover) {
    color: #d1d5db;
  }
</style>